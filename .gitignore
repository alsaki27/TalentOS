# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files (secrets — never commit)
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# supabase CLI local state (regenerated by `supabase link`)
supabase/.temp/

# claude code local settings (per-machine permissions, not project config)
.claude/

# large local-only reference data (not needed to run the app)
dataset_linkedin-jobs-scraper_*.json
