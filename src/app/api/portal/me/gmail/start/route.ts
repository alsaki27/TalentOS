// Canonical authenticated candidate route. Keep the older integrations path
// as a compatibility alias because already-deployed portal links may use it.
export { GET } from "@/app/api/portal/integrations/gmail/start/route";
