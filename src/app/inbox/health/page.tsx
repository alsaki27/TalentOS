"use client";
import { useEffect, useState } from "react";
export default function InboxHealthPage(){const [d,setD]=useState<any>(null); useEffect(()=>{fetch('/api/inbox/health',{cache:'no-store'}).then(r=>r.ok?r.json():null).then(setD).catch(()=>{});},[]); if(!d)return <main className="page-shell"><p className="muted">Loading inbox health…</p></main>; const m=d.mail||{}; return <main className="page-shell" style={{maxWidth:1100}}><p className="page-kicker">Communication intelligence</p><h1>Inbox health</h1><p className="muted">Operational view of Gmail ingestion, triage coverage, suppression, and AE workload.</p><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(170px,1fr))',gap:12,marginTop:20}}>{[['Stored',m.total],['Triaged',m.triaged],['Untriaged',m.untriaged],['Suppressed',m.suppressed],['Relevant',m.relevant],['Gmail accounts',d.sync?.active_accounts]].map(([l,v])=><section className="card" key={String(l)}><small className="muted">{l}</small><h2 style={{marginBottom:0}}>{v??0}</h2></section>)}</div><section className="card" style={{marginTop:20}}><h2>Open AE workload</h2>{(d.tasks||[]).map((x:any)=><div key={x.type} style={{display:'flex',justifyContent:'space-between',borderTop:'1px solid var(--border)',padding:'10px 0'}}><span>{String(x.type).replaceAll('_',' ')}</span><strong>{x.count}</strong></div>)}</section><section className="card" style={{marginTop:20}}><h2>Classification over last 30 days</h2>{(d.categories||[]).map((x:any)=><div key={x.category} style={{display:'flex',justifyContent:'space-between',borderTop:'1px solid var(--border)',padding:'10px 0'}}><span>{String(x.category).replaceAll('_',' ')}</span><strong>{x.count}</strong></div>)}</section></main>}
